{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Bits Runner Builder",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            //"args": ["--verb=v3", "--gen=ir", "${workspaceFolder}/test.brc"],
            "args": ["--verb=v3", "${workspaceFolder}/test.brc", "${workspaceFolder}/lib/b/String.brc"],
            "cwd": "${workspaceFolder}",
            "internalConsoleOptions": "openOnSessionStart",
        }
    ]
}