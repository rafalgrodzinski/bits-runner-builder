{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Bits Runner Builder",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": ["--verb=v3", "--gen=obj", "${workspaceFolder}/test.brc", "${workspaceFolder}/term.brc"],
            //"args": ["--verb=v3", "--gen=obj", "${workspaceFolder}/IntHandler.brc", "${workspaceFolder}/SyscallHandler.brc"],
            "cwd": "${workspaceFolder}",
            "internalConsoleOptions": "openOnSessionStart",
        }
    ]
}