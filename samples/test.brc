@extern putchar fun: character u32 -> u32

print fun: text data<u32, 20>
    text <- "testing\n"
    rep i u32 <- 0, text[i] != 0:
        putchar(text[i])
        i <- i + 1
    ;
;

@export main fun -> u32
    text data<u32, 20> <- "Hello!\n"
    print(text)

    ret 0
;