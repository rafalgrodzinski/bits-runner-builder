engine blob
    size u64
;

car blob
    name data<u64, 16>
    eng blob<engine>
;

@export main fun -> u64
    toyota blob<car>

    toyota.name <- "toyo"
    toyota.eng <- { 1200 }
    //toyota.eng.size <- 1200

    //s u64 <- toyota.eng.size
    toyota.eng.size

    ret 0
;