@module main

@extern putchar fun: character u8 -> u32

@export main fun -> u64
    text data<u8> <- "Hello, world!\n"
    
    rep i u32 <- 0, text[i] != 0, i <- i + 1
        putchar(text[i])
    ;

    ret 0
;