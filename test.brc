@import BSys
@import B

@export main fun -> u32
    ints blob<@B.Array, u32>
    rep i u32, i < 7, i <- i + 1
        ints.push(i * i)
    ;
    rep ints.count > 0
        @B.StringForU32(ints.pop().u32).print()
        @B.String(" ").print()
    ;
    ret 0
;