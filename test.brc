String blob
    string data<u8, 64>

    

    length fun -> u32
        //ret it.string[0]
        i u32 <- 0
        rep it.string[i] != '\0' and i < it.string.count: i <- i + 1
        ret i
    ;
;